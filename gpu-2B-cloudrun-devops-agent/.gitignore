# Most ignores live in the repo-root .gitignore (the git root is gemma4-queens/).
# Keep this file to what is specific to this project.

# Local secrets/overrides; may contain a HF token.
.env

# Benchmark output written into the repo by cloudrun_run_benchmark
benchmark_results.csv

# Python and tool caches
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.log

# Personal, per-developer Claude Code config
CLAUDE.local.md
.claude/settings.local.json
